<% provide(:title,'List of Equipment')%>
<% provide(:name,'List of JOT Equipment')%>
<% provide(:lname1, 'List of Reports')%>
<% provide(:link1, reports_path)%>
<% provide(:lname2, 'Create New Equipment')%>
<% provide(:link2, new_equipment_path)%>
<% provide(:lname3, 'Create New Report')%>
<% provide(:link3, new_report_path)%>


<table width="1200", border="1">
  <thead>
    <tr>
      <th>J-Code</th>
      <th>Serial number</th>
      <th>Date</th>
      <th>Project number</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @equipment.each do |equipment| %>
      <tr>
        <td><%= equipment.jcode %></td>
        <td><%= equipment.serial %></td>
        <td><%= equipment.mdate %></td>
        <td><%= equipment.pnumber %></td>
        <td><%= link_to 'Show', equipment %></td>
        <td><%= link_to 'Edit', edit_equipment_path(equipment) %></td>
        <td><%= link_to 'Destroy', equipment, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

